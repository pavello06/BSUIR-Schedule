import 'package:flutter/material.dart';

import '../../../domain/entities/study_day.dart';

class ScheduleStudyDayWidget extends StatelessWidget {
  const ScheduleStudyDayWidget({super.key, required this.studyDay});

  final StudyDay studyDay;

  @override
  Widget build(BuildContext context) {
    return ;
  }
}
